[package]
name = "nexus-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for Nexus HyperIntelligence"
license = "MIT"

[[example]]
name = "basic_usage"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
